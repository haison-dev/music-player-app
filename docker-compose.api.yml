services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: music-platform-api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 4000
    ports:
      - "4000:4000"
